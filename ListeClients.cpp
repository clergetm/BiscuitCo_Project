#include "ListeClients.h"
/* 
ListeClients::ListeClients() {
	this->queue = this->courant = this->tete = new Noeud<Client>;
}

ListeClients::~ListeClients() {
	while (tete != nullptr) {
		courant = tete;
		tete = tete->suivant;
		delete courant;
	}
}


void ListeClients::Inserer(const Client& client) {
	this->queue = this->queue->suivant = Noeud<Client>(client, nullptr);
};
void ListeClients::Supprimer(const Client&) {};
Client ListeClients::ValeurCourante() {};

void ListeClients::Precedent() {};
void ListeClients::Suivant() {};
void ListeClients::AllerTete() {};
void ListeClients::AllerQueue() {};

bool ListeClients::EstVide() {};
bool ListeClients::VerifierClient(const Client&) {};
*/